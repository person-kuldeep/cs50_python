# there are inbuit functions exist 
print("hello sir") # a function can recognized by seeing the parenthesis 
# we can defiene our own functions also
def my_func(x=44): # we cangive default values to the parameters
    a = 10
    b = 20
    print((a*b)+x) # this is a function definition

#defining a function not enough we need to call it to execute it

my_func() # this is a function call
# we can call a function multiple times
#we can also pass arguments to the function
my_func(5) # passing an argument to the function